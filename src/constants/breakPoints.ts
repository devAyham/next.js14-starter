import vars from "@/styles/variables/_break_points_vars.module.scss";

export const breakPoints = {
  xs: Number(vars.xs),
  sm: Number(vars.sm),
  md: Number(vars.md),
  lg: Number(vars.lg),
  xl: Number(vars.xl),
  xxl: Number(vars.xxl),
};
